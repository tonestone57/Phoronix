diff a/pts-core/objects/phodevi/sensors/cpu_temp.php b/pts-core/objects/phodevi/sensors/cpu_temp.php	(rejected hunks)
@@ -46,6 +46,11 @@ public function read_sensor()
		{
			$temp_c = $this->cpu_temp_linux();
		}
+		else if(phodevi::is_haiku())
+		{
+			// Often CPU temp is exposed via ACPI thermal zones too
+			$temp_c = phodevi_haiku_parser::read_thermal_zone();
+		}

		return $temp_c;
	}
