diff a/pts-core/objects/phodevi/components/phodevi_audio.php b/pts-core/objects/phodevi/components/phodevi_audio.php	(rejected hunks)
@@ -105,6 +105,18 @@ public static function audio_processor_string()
				$audio = phodevi_linux_parser::read_pci('Audio device');
			}
		}
+		else if(phodevi::is_haiku())
+		{
+			$listdev = phodevi_haiku_parser::read_listdev();
+			foreach($listdev as $device)
+			{
+				if(stripos($device['class'], 'Multimedia audio controller') !== false)
+				{
+					$audio = $device['device'];
+					break;
+				}
+			}
+		}

		return $audio;
	}
